# 高校教师项目式教学 (PBL) 备课智能体运行指南

恭喜！您的系统已经开发完成，并集成了 **阿里云 DashScope (通义千问)** 的强大 AI 能力。本系统特别为无编程基础的老师设计，代码中包含了大量中文注释，方便您阅读和学习。

---

## 1. 运行前准备

在开始之前，请确保您的电脑已安装以下软件：
1. **Docker Desktop**: 用于一键启动数据库和整个系统。
2. **OpenAI API Key**: 您已经提供并集成在代码中了（阿里云 API）。

## 2. 如何启动系统

### 第一步：检查配置文件
我已经为您创建好了 `.env` 文件。请确认内容如下：
```env
OPENAI_API_KEY=sk-2f770b9680a84151b9eb297d1a699fac
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
```

### 第二步：一键启动
在项目根目录下（包含 `docker-compose.yml` 的文件夹），打开终端（PowerShell 或 CMD），运行：
```bash
docker-compose up -d
```
*这个命令会自动为您安装数据库、后端程序和前端界面。第一次运行可能需要几分钟时间下载。*

### 第三步：访问系统
- **前端界面**: 打开浏览器访问 `http://localhost:3000`
- **管理后台 (API 文档)**: 访问 `http://localhost:8000/docs`

---

## 3. 教师端使用流程

1. **注册与登录**: 注册时请选择 **“教师”** 角色。
2. **上传知识库**: 
   - 进入“知识库管理”，上传您的 PDF 或 Word 课件。
   - 系统会自动进行“向量化”处理（即把文字变成 AI 能读懂的数字）。
3. **生成 PBL 教案**:
   - 输入您的课程名称和具体教学要求。
   - AI 会根据您上传的知识库内容，生成一份标准的 PBL 教案。
4. **查看学生反馈**:
   - 在“学生反馈”页面，您可以看到学生对 AI 回答的评价。

---

## 4. 学生端使用流程

1. **注册与登录**: 注册时选择 **“学生”** 角色。
2. **智能问答**:
   - 直接向 AI 提问。AI 只会回答知识库里的内容，超出范围会提醒找老师。
   - 回答下方会标注该知识点来源于哪个文档。
3. **评价回答**:
   - 每次回答后，您可以打分（1-5星）并留言。
4. **检索与笔记**:
   - 可以在“资源中心”快速搜索相关文档。
   - 在“学习笔记”记录自己的学习心得。

---

## 5. 代码学习建议（面向无基础人员）

如果您想了解系统是如何工作的，可以优先阅读以下带有详细注释的文件：
- `backend/utils/rag.py`: 了解 AI 是如何检索知识并回答问题的。
- `backend/auth.py`: 了解系统是如何保护您的账号安全的。
- `frontend/src/pages/StudentQA.tsx`: 了解网页上的对话框是如何与 AI 通信的。

---

**遇到问题怎么办？**
- 如果无法启动，请检查 Docker 是否正在运行。
- 如果 AI 不回答，请检查您的阿里云 API 余额是否充足。
